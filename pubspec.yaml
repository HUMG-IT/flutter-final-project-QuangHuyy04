name: bookverse
description: BookVerse - Nguyễn Quang Huy - 2221050558 - DCCTCLC67A - 10/10
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'      # ĐÚNG CHÍNH XÁC – DART 3.5+
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4

  # State & Routing
  flutter_riverpod: ^2.5.1
  go_router: ^14.3.0

  # Features
  fl_chart: ^0.69.0
  google_mlkit_barcode_scanning: ^0.13.0
  image_picker: ^1.1.2
  uuid: ^4.5.0
  http: ^1.2.2

  # Localization – BẮT BUỘC DÙNG intl 0.20.2
  easy_localization: ^3.0.7
  intl: ^0.20.2              # ĐÃ SỔI TỪ ^0.19.0 → ^0.20.2

  # WEB MỚI NHẤT – CHẠY NGON VỚI DART 3.5+
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/translations/